Use the example in the following way
```
    npm install
    node -r esm index.js
```
  
esm is used because the code contains es6 async/await  
  
The example shows simple usages of promises in Javascript. Please see code comments for more details.